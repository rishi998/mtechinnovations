# Used for `next build` / production runtime. Copy to `.env.production` or set in Render.
# Must be the public URL of your Nest API including /api (no trailing slash after /api).
NEXT_PUBLIC_API_URL=https://your-backend.onrender.com/api
